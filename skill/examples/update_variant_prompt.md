# Update Variant Prompt

Update SKU `<sku_id>` for product `<product_id>`.

Change only SKU-level fields in `variants.yaml` unless the fact applies to every variant. Check media references and rerun validation.
