# New Product Prompt

Create a new product knowledge folder for `<product-slug>`.

Use source documents from `sources/`, fill structured product and variant facts first, then write the Markdown modules. Do not invent unsupported claims. Add unresolved questions to `source-notes.md`.

Run validation before finishing.
