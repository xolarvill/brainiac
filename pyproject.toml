[project]
name = "product-kb-system"
version = "0.1.0"
description = "Local-first SKU-level ecommerce product knowledge base template and Codex skill."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["template/tests"]
